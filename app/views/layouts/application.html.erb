<!DOCTYPE html>
<html>
<head>
  <title>Gisty</title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
      <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css">
      <meta name="viewport" content="width=device-width, initial-scale=1">

</head>
<body>
	<div class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <a class="brand" href="/gists">GITpower Team - Exam</a>
    <ul class="nav">
      <li class="active"><a href="/gists">Home</a></li>
      <li class="divider-vertical"></li>
      <li><a href="/gists/new">Nowa wrzutka</a></li>
      <li class="divider-vertical"></li>
      <li class="dropdown">
                <% if current_user %>
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Cześć, <strong><%= current_user.name %><b class="caret"></b></strong></a>
                    <ul class="dropdown-menu">
                        <li><%= link_to "Sign out", signout_path, id: "sign_out" %></li>
                    </ul>
                <% else %>
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Zaloguj<b class="caret"></b></strong></a>
                    <ul class="dropdown-menu">
                        <li><%= link_to "Sign in with GitHub", "/auth/github", id: "sign_in" %></li>
                    </ul>
                <% end %>
            </li>
    </ul>
    <ul class="nav pull-right hidden-xs">
          <form class="navbar-form navbar-left pull-right" role="search" action="/gists/search">
          <div class="form-group">
 
        <%= select_tag(:lang, options_for_select(Pygments.lexers.keys.sort.map! {|l| l.downcase.strip}, params[:lang]))%>
          </div>
          <button type="submit" class="btn btn-default">Szukaj</button>
        </form>
        
        </ul>
      </nav>
  </div>
</div>

<%= yield %>

</body>
</html>
